# Техническое руководство по созданию Telegram-бота для WishList

## Содержание
1. [Введение](#введение)
2. [Требования](#требования)
3. [Установка и настройка](#установка-и-настройка)
4. [Архитектура проекта](#архитектура-проекта)
5. [Пошаговая инструкция](#пошаговая-инструкция)
6. [Примеры использования](#примеры-использования)
7. [Устранение неполадок](#устранение-неполадок)

## Введение

Данное руководство описывает процесс создания Telegram-бота для управления списком желаний (WishList) с использованием Python и фреймворка aiogram. Бот позволяет пользователям создавать, просматривать и управлять своими желаниями, а также просматривать желания других пользователей.

## Требования

### Системные требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)
- SQLite3 (встроена в Python)
- Доступ к интернету
- Токен Telegram-бота

### Необходимые библиотеки
```python
aiogram==3.x
python-dotenv==1.0.0
```

## Установка и настройка

1. Создайте структуру проекта:
```
wishlist-bot/
├── bot.py
├── config.py
├── db_controller.py
├── handlers.py
├── states.py
└── .env
```

2. Установите зависимости:
```bash
pip install aiogram python-dotenv
```

3. Создайте файл `.env`:
```bash
TELEGRAM_TOKEN=ваш_токен_телеграм_бота
```

4. Инициализируйте базу данных:
```python
# Добавьте в bot.py после импортов
db_controller.init_db()
```

5. Запустите бота:
```bash
python bot.py
```

## Архитектура проекта

### Структура проекта
```
wishlist-bot/
├── bot.py           # Основной файл бота
├── config.py        # Конфигурация и загрузка переменных окружения
├── db_controller.py # Работа с базой данных SQLite
├── handlers.py      # Обработчики сообщений и колбэков
├── states.py        # Состояния FSM для диалогов
└── .env             # Конфигурационный файл с токеном
```

### Основные компоненты
1. **Обработчики команд**
   - `/start` - приветственное сообщение и главное меню
   - `/wish_list @username` - просмотр желаний другого пользователя

2. **Диалоговые состояния (FSM)**
   - `AddWishStates` - процесс добавления нового желания
   - `ViewWishStates` - процесс просмотра и управления желаниями

3. **Функции для работы с БД**
   - Управление пользователями, желаниями, группами и резервированиями
   - CRUD-операции для всех сущностей

4. **Инлайн-клавиатуры**
   - Интерактивные меню для навигации
   - Кнопки действий (добавить, удалить, просмотреть)

## Пошаговая инструкция

### 1. Создание бота в Telegram
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен

### 2. Настройка окружения
```python
# config.py
import os
from dotenv import load_dotenv

load_dotenv()
TOKEN = os.getenv('TELEGRAM_TOKEN')
```

### 3. Инициализация бота и диспетчера
```python
# bot.py
from aiogram import Bot, Dispatcher
from aiogram.fsm.strategy import FSMStrategy

async def main():
    dp = Dispatcher(fsm_strategy=FSMStrategy.CHAT)
    bot = Bot(token=config.TOKEN)
    dp.include_router(handlers.router)
    await dp.start_polling(bot)
```

### 4. Создание состояний FSM
```python
# states.py
from aiogram.fsm.state import StatesGroup, State

class AddWishStates(StatesGroup):
    waiting_for_wish_text = State()
    waiting_for_description = State()
    waiting_for_priority = State()
    waiting_for_price = State()
    confirm_wish = State()
```

## Примеры использования

### Добавление желания
1. Нажмите "🎁 Добавить желание"
2. Введите текст желания
3. Добавьте описание (опционально)
4. Выберите приоритет (1-5 ⭐)
5. Укажите стоимость (опционально)
6. Подтвердите сохранение

### Просмотр своих желаний
1. Нажмите "📋 Посмотреть мои желания"
2. Просмотрите список желаний с пагинацией
3. Используйте кнопки навигации для перехода между страницами
4. Нажмите "🗑️ Удалить" для удаления желания

### Просмотр желаний других пользователей
1. Используйте команду `/wish_list @username`
2. Бот отправит список желаний в личные сообщения
3. Просматривайте желания с пагинацией

### Пример взаимодействия
```
Пользователь: /start
Бот: Добро пожаловать в WishList! 🎉
     [Кнопки: Добавить желание, Посмотреть мои желания]

Пользователь: Нажимает "🎁 Добавить желание"
Бот: ✍️ Напишите, что вы хотите добавить в список желаний:

Пользователь: Новый iPhone
Бот: ✅ Записал: "Новый iPhone"
     📝 Добавьте описание (опционально) или нажмите 'Пропустить':

Пользователь: Нажимает "Пропустить"
Бот: ⭐ Выберите приоритет желания (1 = низкий, 5 = высокий):
     [Кнопки: 1⭐, 2⭐, 3⭐, 4⭐, 5⭐]
```

## Устранение неполадок

### Частые проблемы и решения

1. **Ошибка подключения к БД**
   - Проверьте права доступа к файлу базы данных
   - Убедитесь, что SQLite3 установлена и доступна

2. **Ошибка авторизации бота**
   - Проверьте правильность токена в файле `.env`
   - Убедитесь, что бот не был остановлен в BotFather

3. **Бот не отвечает на команды**
   - Проверьте, что роутер handlers подключен к диспетчеру
   - Убедитесь, что обработчики правильно зарегистрированы

4. **Ошибки при работе с FSM**
   - Проверьте правильность импорта состояний
   - Убедитесь, что состояния правильно установлены и очищаются

### Логирование
В случае ошибок проверьте консоль на наличие сообщений об ошибках. Для более детального логирования добавьте:

```python
import logging
logging.basicConfig(level=logging.INFO)
```

### Проверка базы данных
Для проверки состояния базы данных можно использовать SQLite-браузер или выполнить запросы через консоль:

```bash
sqlite3 wishlist.db
.tables
SELECT * FROM user LIMIT 5;
```

### Особенности реализации
- Бот использует стратегию FSM по чатам (`FSMStrategy.CHAT`)
- Все данные пользователей сохраняются автоматически при первом использовании
- Поддерживается работа как в личных чатах, так и в группах
- Реализована пагинация для удобного просмотра длинных списков
